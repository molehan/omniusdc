# Apps

Frontend(s) live here.

V1 typically starts with a single web app (e.g., Next.js) that:
- lets users deposit USDC on the chosen L2 source chain
- shows L1 vault position (shares, NAV)
- lets users sign WithdrawIntent (EIP-712)

Implementation is intentionally deferred in Step 1.
