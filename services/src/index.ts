export function main(): void {
  // Placeholder entrypoint. Actual relayer/attestation/monitoring services will live here.
  // Keep this file minimal to ensure CI can compile from day one.
  // eslint-disable-next-line no-console
  console.log("OmniUSDC services placeholder");
}

if (require.main === module) {
  main();
}
